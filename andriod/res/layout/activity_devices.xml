<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity" >

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:orientation="horizontal" >
    </LinearLayout>

    <LinearLayout
        android:id="@+id/linearLayout2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:orientation="vertical" >

    </LinearLayout>

    <ListView
        android:id="@+id/ListView_Simple"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_above="@+id/Button_Scan"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/TextView_BTLED" >

    </ListView>

    <Button
        android:id="@+id/Button_Scan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="Scan for Devices" />

    <ProgressBar
        android:id="@+id/BLEDD_ProgressBar"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ListView_Simple"
        android:layout_alignTop="@+id/TextView_BTLED"
        android:layout_toRightOf="@+id/TextView_BTLED"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/TextView_BTLED"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="Discovering Devices. ."
        android:textSize="24sp" />

</RelativeLayout>